export {}

describe('example', () => {
  test('Math.sqrt()', () => {
    expect(Math.sqrt(4)).toBe(2)
  })

  test('Math.pow()', () => {
    expect(Math.pow(2, 2)).toBe(4)
  })

  test('Math.max()', () => {
    expect(Math.max(1, 5, 10)).toBe(10)
  })
})
